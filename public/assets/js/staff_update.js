function setForm(tid, image, name, weight, quantity, activity, condition){
    $('#tid').val(tid);
    $('#eimage').val(image);
    $('#ename').val(name);
    $('#eweight').val(weight);
    $('#equantity').val(quantity);
    $('#eactivity').val(activity);
    $('#econdition').val(condition);
}